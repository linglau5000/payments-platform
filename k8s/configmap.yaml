apiVersion: v1
kind: ConfigMap
metadata:
  name: payments-config
  namespace: payments
data:
  # Non-secret runtime configuration — secrets go in k8s/secret.yaml
  POLL_INTERVAL_SECONDS: "10"
  LOG_LEVEL: "INFO"
