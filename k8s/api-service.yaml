apiVersion: v1
kind: Service
metadata:
  name: payments-api-service
  namespace: payments
spec:
  selector:
    app: payments-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP   # Exposed externally via Ingress or LoadBalancer in production
